// import React from 'react';
import { Input } from "antd";

export default class SearchDL extends Input.Search {
  /*
  getButtonOrIcon() {
    const node = super.getButtonOrIcon();
    return React.cloneElement(node, {
        type: "download",
      });
  }
  */
}
